# Screenshots

Drop three PNGs here, then uncomment the block in the top-level `README.md`
("Screenshots" section). Use these exact filenames so the references resolve:

| File | What to capture |
|------|-----------------|
| `01-home.png` | The **Home / study overview** — the four stat cards and the amber "1 decision is missing a rationale" banner. |
| `02-ask.png` | An **Ask** answer — a reconstructed "why", with a provenance chip visible on a claim (e.g. *"Why did we drop the LPS dose in cohort 3?"*). |
| `03-open-questions.png` | The **Open Questions** inbox — the proactive-interview prompt for the Gal-3 antibody swap. |

Tips:
- Load the sample study first so the shots are populated: `uv run python scripts/demo.py --seed`.
- Capture at a consistent width (a ~1200px-wide window looks good scaled to 900).
- Light theme tends to reproduce best in a README; the toggle is in the top bar.
